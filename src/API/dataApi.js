import baseUrl from './baseUrl.js'

const dataApi = {
  behaviour: `${baseUrl}/statistics/save`,
  clickpopup: `${baseUrl}/statistics/clickpopup`,
  associateClickCount: `${baseUrl}/association/click`
}

export default dataApi
